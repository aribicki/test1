// Andrew Ribicki
// CMP237 test1
// write a program that declares an array of objects, and then does soemthing to extract and print information of any sort from an array
 
 var list = [
	{"entry": ["car" , "red" , "small"]
	{"entry": ["truck" , "blue" , "big"]
        {"entry": ["car" , "yellow" , "small"]
];
 var n = list.length; // create variable n and set it = to the lenght of the list
  console.log(n) // print the length of the list
